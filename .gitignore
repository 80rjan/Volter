# Ignore .env files (for environment variables)
**/.env*
# ...except the templates, which carry no secrets and belong in the repo.
!**/.env*.example
# Per-environment compose secrets (the *.example templates ARE committed).
node_modules
frontend/dist
/dist
.idea