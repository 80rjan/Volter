# Ignore .env files (for environment variables)
**/.env*
# Per-environment compose secrets (the *.example templates ARE committed).
node_modules
frontend/dist
/dist
.idea
# Kubernetes: the real Secret is applied locally; only the *.example template is committed.
