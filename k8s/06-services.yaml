apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: volter
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: volter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: volter
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: volter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
