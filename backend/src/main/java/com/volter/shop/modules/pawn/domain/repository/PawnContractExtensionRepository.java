package com.volter.shop.modules.pawn.domain.repository;

import com.volter.shop.modules.pawn.domain.model.PawnContractExtension;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PawnContractExtensionRepository extends JpaRepository<PawnContractExtension, Long> {
}
